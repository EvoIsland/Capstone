import { FastifyInstance } from 'fastify';
import fp from 'fastify-plugin';

// Este plugin se mantiene para futuras funcionalidades de autenticación
export default fp(async function (fastify: FastifyInstance) {
    // Aquí irán futuras decoraciones y utilidades de autenticación
});
